# No Rollback

When context needs to change from A to B:
- Fix errors in B
- Never suggest going back to A
- Never revert concepts, architectures, models, or patterns already changed
- Focus on solving problems in solution B

Applies to: languages, frameworks, architectures, patterns, libraries — any change the user requested.
