# Language

When communicating with the user:
- Always respond in the language the user uses
- Match the user's language throughout the conversation

When creating or updating rules:
- Write rule files exclusively in English
- Never write rules in other languages
